# RClick 架构重构实施计划

> 最后更新：2026-03-21
> 状态：实施计划
> 基于：瘦 Extension 架构设计

---

## 一、计划摘要

本计划描述了 RClick 架构重构的详细实施步骤，目标是实现**瘦 Extension 架构**，将 FinderSync 扩展简化为纯渲染层。

### 优先级说明

| 优先级 | 说明 |
|--------|------|
| P0 | 最高优先级，核心功能，必须完成 |
| P1 | 高优先级，重要优化 |
| P2 | 中优先级，改进体验 |

---

## 二、实施阶段

### Phase 1: 类型安全通信协议 ⭐⭐⭐ P0

**目标**: 消除字符串硬编码，提升类型安全性，支持消息追踪

**预计时间**: 2-3 小时

**风险**: 低

#### 步骤 1.1: 创建通信协议文件

**文件**: `RClick/Shared/CommunicationProtocol.swift`

**内容**:
- 定义 `MainToExtensionAction` 枚举
- 定义 `ExtensionToMainAction` 枚举
- 定义 `MenuConfigPayload`、`ClickEventPayload` 等数据结构
- 添加消息 ID 支持（`UUID`）

#### 步骤 1.2: 更新 Messager 类

**文件**: `RClick/Shared/Messager.swift`

**修改内容**:
- 重构为支持类型安全的枚举消息
- 添加请求/响应模式支持
- 添加超时检查机制（可选）

#### 步骤 1.3: 更新 FinderSyncExt

**文件**: `FinderSyncExt/FinderSyncExt.swift`

**修改内容**:
- 替换所有字符串比较为枚举比较
- 更新消息处理逻辑

#### 步骤 1.4: 更新 RClickApp

**文件**: `RClick/RClickApp.swift`

**修改内容**:
- 使用枚举创建消息
- 更新消息分发逻辑

#### 验收标准

- [ ] 编译无错误
- [ ] 所有消息使用枚举类型
- [ ] 消息 ID 正确生成和追踪

---

### Phase 2: 瘦 Extension 架构 ⭐⭐⭐ P0

**目标**: Extension 只负责渲染和事件转发

**预计时间**: 2-3 小时

**风险**: 中等

#### 步骤 2.1: 更新 FinderSyncExt

**文件**: `FinderSyncExt/FinderSyncExt.swift`

**修改内容**:
- 删除 SwiftData 读取逻辑
- 添加 `menuConfig` 内存缓存
- 实现 `setupMessageHandlers()`
- 实现纯渲染的 `menu(for:)` 方法
- 实现全盘监听逻辑（`/Users/` + 外接磁盘）

#### 步骤 2.2: 更新 AppState

**文件**: `RClick/AppState.swift`

**修改内容**:
- 添加 `buildMenuConfig()` 方法
- 删除 `dirs`（许可目录）相关的属性和方法
- 删除 `notifyExtensionMenuUpdate()` 方法及相关调用

#### 步骤 2.3: 更新 RClickApp

**文件**: `RClick/RClickApp.swift`

**修改内容**:
- 添加 `sendMenuConfig()` 方法
- 添加 `handleClickEvent()` 方法

#### 验收标准

- [ ] Extension 不读取任何 SwiftData
- [ ] 菜单配置由主程序构建并发送
- [ ] Extension 正确渲染菜单

---

### Phase 3: 优化 Tag 管理 ⭐⭐ P1

**目标**: 消除随机数冲突风险

**预计时间**: 30 分钟

**风险**: 极低

#### 步骤 3.1: 修改 Tag 生成逻辑

**文件**: `FinderSyncExt/FinderSyncExt.swift`

**修改内容**:
- 添加 `nextTag` 计数器
- 将随机 tag 改为递增 tag

**修改前**:
```swift
private func getUniqueTag(for rid: String) -> Int {
    var newTag = Int.random(in: 1 ... Int.max)
    while tagRidDict.keys.contains(newTag) {
        newTag = Int.random(in: 1 ... Int.max)
    }
    tagRidDict[newTag] = rid
    return newTag
}
```

**修改后**:
```swift
private var nextTag: Int = 1

private func getUniqueTag(for rid: String) -> Int {
    let tag = nextTag
    nextTag += 1
    tagRidDict[tag] = rid
    return tag
}
```

#### 验收标准

- [ ] Tag 使用递增方式生成
- [ ] 无随机数冲突风险

---

### Phase 4: 统一 Icon 缓存 ⭐⭐ P1

**目标**: 消除重复代码

**预计时间**: 1 小时

**风险**: 低

#### 步骤 4.1: 创建统一 Icon 缓存

**文件**: `RClick/Shared/IconCache.swift`

**内容**:
```swift
@MainActor
public class IconCache {
    public static let shared = IconCache()

    private var memoryCache: [String: NSImage] = [:]
    private let iconSize = CGSize(width: 32, height: 32)

    private init() {}

    public func icon(for url: URL) -> NSImage {
        let cacheKey = url.path
        if let cached = memoryCache[cacheKey] {
            return cached
        }

        let icon = NSWorkspace.shared.icon(forFile: url.path)
        icon.size = iconSize
        memoryCache[cacheKey] = icon
        return icon
    }

    public func preloadIcons(for urls: [URL]) {
        for url in urls {
            _ = icon(for: url)
        }
    }

    public func clearCache() {
        memoryCache.removeAll()
    }
}
```

#### 步骤 4.2: 删除重复实现

**文件**:
- `FinderSyncExt/FinderSyncExt.swift` - 删除 IconCache 类
- `RClick/Shared/Messager.swift` - 删除 IconCacheManager

#### 步骤 4.3: 使用统一缓存

**修改内容**:
- 所有图标加载使用 `IconCache.shared`

#### 验收标准

- [ ] 只有一个 IconCache 实现
- [ ] 图标加载正常工作

---

### Phase 5: 全盘访问权限 ⭐⭐ P1

**目标**: 请求完全磁盘访问权限，无需 Bookmark 缓存

**预计时间**: 30 分钟

**风险**: 低

#### 步骤 5.1: 添加完全磁盘访问权限引导

**文件**: `RClick/Settings/GeneralSettingsTabView.swift`

**修改内容**:
- 添加「完全磁盘访问权限」按钮
- 点击后打开系统设置对应页面
- 显示授权状态

```swift
@State private var hasFullDiskAccess = false

private func checkFullDiskAccess() -> Bool {
    // 尝试访问受保护目录检测权限
    let path = "/Library/Application Support"
    return FileManager.default.isReadableFile(atPath: path)
}

private func openFullDiskAccessSettings() {
    NSWorkspace.shared.open(URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_AllFiles")!)
}
```

#### 步骤 5.2: 更新 entitlements 配置

**文件**: `RClick/RClick.entitlements`

**说明**: 不需要额外的 entitlement，完全磁盘访问权限由用户手动授权

#### 验收标准

- [ ] 设置界面显示完全磁盘访问权限状态
- [ ] 点击按钮可跳转到系统设置
- [ ] 授权后功能正常工作

---

### Phase 6: Extension 重连机制 ⭐⭐ P1

**目标**：解决主 App 已打开但 Extension 未加载的问题

**预计时间**：2-3 小时

**风险**：低

#### 设计原则（必须遵守）

| 原则 | 说明 | 违反后果 |
|------|------|----------|
| **Notification ≠ 状态** | 通知只能当"信号"，不能当"真相" | 丢消息=状态错乱 |
| **必须存在状态快照** | 否则无法恢复一致性 | 重启后数据丢失 |
| **Extension 必须支持主动拉取** | 不能只靠推送 | 无法自愈 |
| **所有通信必须幂等** | 重复消息不能出错 | 重复执行风险 |

#### 步骤 6.1: 状态快照机制（P0）⭐ 核心

**文件**：`RClick/RClickApp.swift`

**修改内容**：
- 添加 `lastMenuSnapshot: Data?` 和 `menuVersion: Int`
- 每次菜单配置变更时更新快照和版本号
- 注册 `requestConfig` 消息处理器

**核心设计**：

```swift
// 状态快照（真相源）
private var lastMenuSnapshot: Data?
private var menuVersion: Int = 0

// 状态变更时
func updateMenuConfig() {
    menuVersion += 1
    let snapshot = MenuSnapshot(version: menuVersion, config: buildMenuConfig())
    lastMenuSnapshot = try? JSONEncoder().encode(snapshot)
    // 发送 running 消息时携带 snapshot
    sendRunningMessage(snapshot)
}

// 响应 Extension 的 config 请求
func handleConfigRequest() {
    if let data = lastMenuSnapshot {
        Messager.shared.sendToExtension(.menuConfig, data: data)
    }
}
```

**running 消息携带 MenuSnapshot**：

```swift
func sendRunningMessage(_ snapshot: MenuSnapshot) {
    Messager.shared.sendToExtension(.running, data: snapshot)
}
```

#### 步骤 6.2: 主动请求机制（P0）⭐ 核心

**文件**：`FinderSyncExt/FinderSyncExt.swift`

**修改内容**：
- 添加 `sendMenuRequest()` 方法
- `init()` 中启动时发送请求
- `menu(for:)` 中缓存为空时触发请求
- 接收更新时检查版本号（防重复、防乱序）

```swift
class FinderSyncExt: FIFinderSync {
    private var currentVersion: Int = 0
    private var cachedMenuConfig: MenuConfigPayload?

    override init() {
        super.init()
        setupMessageHandlers()
        // 启动时主动请求配置
        sendMenuRequest()
    }

    private func handleRunning(_ data: Data?) {
        guard let snapshot = decode<MenuSnapshot>(data) else { return }
        // 版本检查：只接受更新的版本
        guard snapshot.version > currentVersion else { return }
        currentVersion = snapshot.version
        cachedMenuConfig = snapshot.config
    }

    private func handleMenuConfig(_ data: Data?) {
        guard let config = decode<MenuConfigPayload>(data) else { return }
        cachedMenuConfig = config
    }

    // 菜单构建：只读缓存
    override func menu(for menuKind: FIMenuKind) -> NSMenu {
        guard let config = cachedMenuConfig else {
            // 缓存为空，触发请求
            sendMenuRequest()
            return NSMenu(title: "RClick (loading...)")
        }
        // 使用缓存构建菜单...
    }
}
```

#### 步骤 6.3: 心跳机制（P1）

**文件**：`RClick/RClickApp.swift` 和 `FinderSyncExt/FinderSyncExt.swift`

**修改内容**：
- **Main App 端**：
  - 在 `applicationDidFinishLaunching` 中发送 `running` 消息（携带 `MenuSnapshot`）
  - 启动重试定时器：每 5 秒发送一次，持续 30 秒（6 次）
  - 注册 `heartbeat` 消息处理器，重置超时定时器（15 秒）
  
- **Extension 端**：
  - 启动定时器：每 10 秒发送 `heartbeat` 给主程序

```swift
// Main App 端
private var heartbeatTimeoutTimer: Timer?
private var isExtensionOnline: Bool = false

func handleHeartbeat() {
    isExtensionOnline = true
    // 重置超时定时器
    heartbeatTimeoutTimer?.invalidate()
    heartbeatTimeoutTimer = Timer.scheduledTimer(withTimeInterval: 15, repeats: false) { [weak self] _ in
        self?.handleHeartbeatTimeout()
    }
}

// Extension 端
Timer.scheduledTimer(withTimeInterval: 10, repeats: true) { _ in
    Messager.shared.sendToMain(.heartbeat)
}
```

#### 步骤 6.4: 自动重连机制（P1）

**文件**：`RClick/RClickApp.swift`

**修改内容**：
- 添加心跳超时检测（15 秒）
- 超时后标记 Extension 为离线状态，菜单栏显示"扩展未响应"

```swift
// Main App 端 - 心跳超时处理
private func handleHeartbeatTimeout() {
    // 15 秒未收到心跳，标记 Extension 离线
    isExtensionOnline = false
    // 菜单栏显示"扩展未响应"
    // 用户可点击"重新加载扩展"按钮
}

// 用户点击"重新加载扩展"时
private func refreshExtension() {
    // 发送 running + menuConfig，尝试重新连接
    sendRunningMessageWithRetry()
}
```

**心跳方向说明**：

| 方向 | 消息类型 | 频率 | 目的 |
|------|----------|------|------|
| Main → Ext | `running` | 启动时 + 重试 6 次 | 告知 Extension 主程序已启动 |
| Ext → Main | `heartbeat` | 每 10 秒 | 告知主程序 Extension 存活 |

#### 步骤 6.5: 菜单强制刷新（P2）

**文件**：`RClick/Settings/MenuBarView.swift` 或菜单栏组件

**修改内容**：
- 在菜单栏添加"刷新 Extension"按钮
- 实现 `refreshExtension()` 方法，发送 `running` + `MenuSnapshot`

```swift
// 用户点击"刷新 Extension"时调用
private func refreshExtension() {
    // 重新发送 running 消息，携带当前快照
    if let snapshot = lastMenuSnapshot {
        Messager.shared.sendToExtension(.running, data: snapshot)
    }
}
```

#### 验收标准

- [ ] 主 App 保存最后菜单状态（快照）
- [ ] Extension 启动后主动请求菜单
- [ ] 版本检查正常工作（防重复、防乱序）
- [ ] 缓存为空时自动请求
- [ ] 主 App 启动后发送 6 次 `running` 消息（携带 MenuSnapshot）
- [ ] Extension 每 10 秒发送 `heartbeat` 给主 App
- [ ] 主 App 15 秒未收到心跳，标记 Extension 离线

---

### Phase 7: 清理和验证 ⭐ P2

**目标**：删除旧代码，验证功能

**预计时间**：1 小时

**风险**：无

#### 步骤 7.1: 删除旧代码

**删除内容**:
1. FinderSyncExt 中的 IconCache 类
2. Messager 中的 IconCacheManager 类
3. ActionMenuItem 和 AppMenuItem 转换代码
4. `notifyExtensionMenuUpdate()` 方法
5. UserDefaults 中的菜单配置存储逻辑
6. PermDirEntity / PermissiveDirEntity 相关的 UI 设置代码

#### 步骤 7.2: 功能验证

**测试清单**:
- [ ] 编译测试
- [ ] 功能测试
- [ ] 性能测试

---

## 三、验证计划

### 3.1 编译测试

```bash
# 清理构建
xcodebuild clean -project RClick.xcodeproj -scheme RClick

# 编译 Main App
xcodebuild build -project RClick.xcodeproj -scheme RClick -destination 'platform=macOS'

# 编译 Extension
xcodebuild build -project RClick.xcodeproj -scheme FinderSyncExt -destination 'platform=macOS'
```

### 3.2 功能测试清单

#### Extension 启动测试

- [ ] Extension 成功初始化
- [ ] 发送 heartbeat 给主程序
- [ ] 接收主程序发送的菜单配置
- [ ] **全盘监听**: 验证 `/Users/` 被添加为监听目录
- [ ] **外接磁盘**: 插入外接磁盘，验证自动添加为监听目录

#### 菜单创建测试

- [ ] 右键菜单显示正确的动作项
- [ ] 右键菜单显示正确的应用项
- [ ] 图标正确加载

#### 配置更新测试

- [ ] 在 Main App 中添加新应用，Extension 菜单更新
- [ ] 在 Main App 中修改动作配置，Extension 菜单更新

#### 动作执行测试

- [ ] 点击"Copy Path"正确复制路径
- [ ] 点击应用正确打开文件
- [ ] Tag 正确关联到动作

#### 权限处理测试

**完全磁盘访问权限模式**（无需 Bookmark 缓存）：

- [ ] **完全磁盘访问权限**：设置界面显示权限状态
- [ ] **权限引导**：点击按钮可跳转到系统设置
- [ ] **权限检测**：授权后功能正常工作
- [ ] **无 Bookmark 缓存**：确认代码中已删除 Bookmark 相关逻辑

#### 错误处理测试

- [ ] Main App 未启动时 Extension 行为
- [ ] 消息丢失时的降级处理
- [ ] Bookmark 过期时的自动刷新

### 3.3 性能测试

- [ ] 菜单创建时间：< 50ms
- [ ] 图标加载时间：< 50ms/个
- [ ] Extension 内存使用：< 20MB
- [ ] 消息传递延迟：< 100ms

### 3.4 日志检查

查看 Console.app，验证：

- [ ] Extension 成功接收菜单配置
- [ ] 菜单数据正确加载
- [ ] 消息正确发送和接收
- [ ] 错误被正确处理和记录

---

## 四、关键文件清单

### 需要创建的文件

1. `RClick/Shared/CommunicationProtocol.swift` - 类型安全的通信协议
2. `RClick/Shared/IconCache.swift` - 统一 Icon 缓存

### 需要修改的文件

1. **FinderSyncExt/FinderSyncExt.swift**
   - 删除 IconCache 类
   - 删除 SwiftData 读取逻辑
   - 实现全盘监听逻辑
   - 添加 `menuConfig` 内存缓存
   - 实现 `setupMessageHandlers()`
   - 实现纯渲染的 `menu(for:)` 方法
   - 优化 Tag 管理（使用递增 ID）
   - 使用类型安全的枚举

2. **RClick/Shared/Messager.swift**
   - 重构为支持枚举消息
   - 删除 IconCacheManager

3. **RClick/AppState.swift**
   - 添加 `buildMenuConfig()` 方法
   - 删除 `notifyExtensionMenuUpdate()` 方法
   - 删除 `dirs` 相关属性和方法

4. **RClick/RClickApp.swift**
   - 使用类型安全的枚举
   - 添加 `sendMenuConfig()` 方法
   - 添加 `handleClickEvent()` 方法
   - 添加完全磁盘访问权限检测逻辑
   - **新增**：心跳重试机制（`startRunningMessageRetry()`）
   - **新增**：心跳超时检测（`checkHeartbeat()`）

5. **RClick/Settings/MenuBarView.swift** 或菜单构建处
   - 添加"刷新 Extension"按钮

---

## 五、风险评估与缓解

### 5.1 破坏性变更

**风险**: 通信协议重构可能影响兼容性

**级别**: 中等

**缓解**:
- Enum 的 rawValue 保持与原有字符串相同
- 支持向后兼容的消息解析
- 逐步迁移，保留过渡期

### 5.2 消息同步

**风险**: 主程序和 Extension 消息不同步

**级别**: 中等

**缓解**:
- 使用心跳机制确认连接
- 主程序启动时主动发送配置
- Extension 定期请求配置刷新

### 5.3 性能影响

**风险**: 消息传递可能比直接读取慢

**级别**: 低

**缓解**:
- 使用内存缓存，减少消息频率
- 只在配置变更时发送完整配置
- 实测性能，优化消息频率

---

## 六、时间估算

| 阶段 | 预计时间 | 优先级 |
|------|----------|--------|
| Phase 1: 类型安全通信协议 | 2-3 小时 | P0 |
| Phase 2: 瘦 Extension 架构 | 2-3 小时 | P0 |
| Phase 3: 优化 Tag 管理 | 30 分钟 | P1 |
| Phase 4: 统一 Icon 缓存 | 1 小时 | P1 |
| Phase 5: 全盘访问权限 | 30 分钟 | P1 |
| Phase 6: Extension 重连机制 | 1-2 小时 | P1 |
| Phase 7: 清理和验证 | 1 小时 | P2 |
| **总计** | **8-11 小时** | - |

---

## 七、参考文档

- **重构设计**: [../重构设计.md](../重构设计.md)
- **通信协议**: [contracts/communication-protocol.md](contracts/communication-protocol.md)
- **数据模型**: [contracts/data-models.md](contracts/data-models.md)
- **Extension 重连**: [contracts/extension-reconnect.md](contracts/extension-reconnect.md)

---

## 八、脚本执行功能实施计划

### Phase 8: 脚本执行功能 ⭐⭐ P1

**目标**: 支持用户自定义脚本，通过右键菜单执行

**预计时间**: 3-4 小时

**风险**: 低

#### 步骤 8.1: 创建 ScriptEntity

**文件**: `RClick/Model/ScriptEntity.swift`

**内容**:
```swift
@Model
final class ScriptEntity {
    @Attribute(.unique) var id: String
    var name: String
    var scriptType: String        // "shell" | "python" | "applescript"
    var scriptContent: String
    var icon: String
    var sortOrder: Int
    var isEnabled: Bool
}
```

#### 步骤 8.2: 添加 RCScript 内存模型

**文件**: `RClick/Model/RCBase.swift`

**修改内容**:
- 添加 `RCScript` 结构
- 添加 `ScriptType` 枚举
- 添加 `RCScript` ↔ `ScriptEntity` 转换扩展

#### 步骤 8.3: 添加 ScriptMenuItem 消息模型

**文件**: `RClick/Shared/CommunicationProtocol.swift`

**修改内容**:
- 添加 `ScriptMenuItem` 结构
- 在 `MenuConfigPayload` 中添加 `scripts: [ScriptMenuItem]`
- 在 `MenuItemType` 中添加 `case script`

#### 步骤 8.4: 更新 AppState

**文件**: `RClick/AppState.swift`

**修改内容**:
- 添加 `@Published var scripts: [RCScript]`
- 添加 `save()` 和 `load()` 方法
- 添加 `buildMenuConfig()` 方法中构建脚本菜单

#### 步骤 8.5: 更新 RClickApp

**文件**: `RClick/RClickApp.swift`

**修改内容**:
- 添加 `executeScript()` 方法
- 实现 Shell 脚本执行
- 实现 Python 脚本执行
- 实现 AppleScript 执行
- 在 `handleClickEvent()` 中处理 script 类型点击

#### 步骤 8.6: 更新 FinderSyncExt

**文件**: `FinderSyncExt/FinderSyncExt.swift`

**修改内容**:
- 在菜单中添加"执行脚本"项（二级菜单）
- 渲染脚本子菜单
- 点击时发送 `click` 事件（type 为 `script`）

#### 步骤 8.7: 创建脚本设置界面

**文件**: `RClick/Settings/ScriptsSettingsTabView.swift`

**内容**:
- 脚本列表（名称、类型、启用状态）
- 添加/编辑/删除脚本
- 上下拖动排序
- 脚本编辑器（多行文本框）
- 测试按钮

#### 验收标准

- [ ] ScriptEntity 正确创建和存储
- [ ] 脚本配置正确保存和加载
- [ ] 右键菜单显示"执行脚本"二级菜单
- [ ] 点击脚本正确执行
- [ ] Shell 脚本执行正常
- [ ] Python 脚本执行正常
- [ ] AppleScript 执行正常
- [ ] 设置界面可添加/编辑/删除脚本
- [ ] 脚本按 sortOrder 排序
- [ ] 编译无错误和警告

### 8.1 脚本执行安全考虑

**安全限制**：
1. 脚本以当前用户权限运行（无 sudo）
2. 脚本执行前不经过沙盒限制（需要完全磁盘访问权限）
3. 用户自行承担脚本执行风险

**建议**：
- 在设置界面添加安全提示
- 提供脚本执行日志
- 支持脚本执行超时（默认 30 秒）

### 8.2 脚本模板

**预定义脚本模板**（可选）：
| 名称 | 类型 | 说明 |
|------|------|------|
| 显示文件信息 | shell | `mdls "$1"` |
| 在 VSCode 打开 | shell | `open -a "Visual Studio Code" "$@"` |
| 获取文件哈希 | shell | `shasum -a 256 "$@"` |
| 批量重命名（小写） | shell | `for f in "$@"; do mv "$f" "${f,,}"; done` |

用户可以从模板快速创建脚本，然后修改内容。

---

## 九、时间估算汇总

| 阶段 | 预计时间 | 优先级 |
|------|----------|--------|
| Phase 1: 类型安全通信协议 | 2-3 小时 | P0 |
| Phase 2: 瘦 Extension 架构 | 2-3 小时 | P0 |
| Phase 3: 优化 Tag 管理 | 30 分钟 | P1 |
| Phase 4: 统一 Icon 缓存 | 1 小时 | P1 |
| Phase 5: 全盘访问权限 | 30 分钟 | P1 |
| Phase 6: Extension 重连机制 | 1-2 小时 | P1 |
| Phase 7: 清理和验证 | 1 小时 | P2 |
| **Phase 8: 脚本执行功能** | **3-4 小时** | **P1** |
| **总计** | **11-15 小时** | - |
